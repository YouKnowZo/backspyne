import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.paperbagexpress.backspyne',
  appName: 'BackSpyne',
  webDir: 'dist'
};

export default config;
